{
  "scalar": null,
  "gates": [
    {
      "name": "G1_file_structure",
      "cmd": "test -f index.html && grep -q '<!DOCTYPE html>' index.html",
      "must": true
    },
    {
      "name": "G2_required_sections",
      "cmd": "grep -q -i 'currency' index.html && grep -q -i 'stripe' index.html && grep -q -i 'pricing' index.html && grep -q -i 'faq' index.html",
      "must": true
    },
    {
      "name": "G3_interactive_script",
      "cmd": "test -f app.js",
      "must": true
    }
  ],
  "termination": {
    "max_iters": 5
  }
}